# Externalized Assets

This repository does not bundle golden validation outputs in the public Option B layout.

For local validation, keep golden outputs in an external assets directory such as:

```text
.\.leaf-measure-assets\golden\
```

