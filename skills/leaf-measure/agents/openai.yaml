interface:
  display_name: "Leaf Measure"
  short_description: "Measure leaf traits from image folders with FAMeLeS mode selection and repair-aware routing."
  default_prompt: "Use $leaf-measure to explain Full image vs Thumbnails, choose the right mode, and measure leaf area, perimeter, length, width, circularity, and solidity for this folder."

policy:
  allow_implicit_invocation: true
