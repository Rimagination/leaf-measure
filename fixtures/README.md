# Externalized Assets

This repository does not bundle upstream trial input files in the public Option B layout.

If you need local validation inputs, stage them into `.leaf-measure-assets/` or another external assets directory and point the CLI to that assets root.

